/mvnw text eol=lf
*.cmd text eol=crlf
# Shell scripts must keep LF, or the shebang becomes '/usr/bin/env bash\r' and the
# script dies with "No such file or directory" on Linux, CI included.
*.sh text eol=lf
